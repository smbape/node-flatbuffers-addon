{
  "name": "node-flatbuffers",
  "version": "0.0.0",
  "description": "Node flatbuffers addon",
  "engines": {
    "node": ">=6.0.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "_mocha --full-trace --require tests/common.js tests/",
    "download-prerequisites": "node scripts/download-prerequisites.js",
    "cmake-build": "cmake-js build",
    "cmake-install": "npm run download-prerequisites && cmake-js compile",
    "build": "node-gyp build",
    "install": "npm run download-prerequisites && node-gyp rebuild"
  },
  "author": "Stéphane MBAPE <smbape@yahoo.fr>",
  "license": "MIT",
  "dependencies": {
    "anyspawn": "^1.1.1",
    "bindings": "^1.3.0",
    "cmake-js": "^3.6.2",
    "mkdirp": "^0.5.1",
    "nan": "^2.10.0",
    "node-gyp": "^3.6.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "flatbuffers": "^1.9.0",
    "mocha": "^5.1.1"
  }
}
