{
  "name": "flatbuffers-addon",
  "version": "0.0.1",
  "description": "Node flatbuffers addon",
  "keywords": [
    "flatbuffers"
  ],
  "engines": {
    "node": ">=6.0.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "node node_modules/mocha/bin/_mocha --full-trace --require tests/common.js \"tests/*-test.js\"",
    "prepack": "npm run test",
    "download-prerequisites": "node scripts/download-prerequisites.js",
    "build": "node-gyp build",
    "install": "npm run download-prerequisites && node-gyp rebuild"
  },
  "author": "Stéphane MBAPE <smbape@yahoo.fr>",
  "license": "MIT",
  "dependencies": {
    "anyspawn": "^1.1.1",
    "bindings": "^1.3.0",
    "mkdirp": "^0.5.1",
    "nan": "^2.10.0",
    "node-gyp": "^3.6.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "flatbuffers": "^1.9.0",
    "mocha": "^5.1.1"
  }
}
