{
  "name": "flatbuffers-addon",
  "version": "1.0.3",
  "description": "Node flatbuffers addon",
  "repository": {
    "type": "git",
    "url": "https://github.com/smbape/node-flatbuffers-addon"
  },
  "keywords": [
    "flatbuffers"
  ],
  "engines": {
    "node": ">=8.0.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "_mocha --full-trace \"tests/*-test.js\"",
    "test-embind": "_mocha --full-trace tests/switch-to-embind.js \"tests/*-test.js\"",
    "prepublishOnly": "echo \"Error: this branch should not be published\" && exit 1",
    "rebuild": "node scripts/clean.js && npm run install && npm run test && npm run test-embind",
    "download-prerequisites": "node scripts/download-prerequisites.js",
    "install": "npm run download-prerequisites && node-gyp rebuild && node scripts/embind.js"
  },
  "author": "Stéphane MBAPE <smbape@yahoo.fr>",
  "license": "MIT",
  "dependencies": {
    "async": "^3.2.0",
    "bindings": "^1.5.0",
    "diff-match-patch": "^1.0.4",
    "mkdirp": "^1.0.3",
    "node-gyp": "^6.1.0",
    "request": "^2.88.2",
    "tar": "^6.0.1"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "chai": "^4.2.0",
    "flatbuffers": "^1.12.0",
    "mocha": "^7.1.0",
    "rimraf": "^3.0.2"
  }
}
